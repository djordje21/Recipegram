<strong><%= comment.comment %></strong>- <%= time_ago_in_words(comment.created_at)  %> ago <br>
	

	<!-- Ne znam zasto mi ne radi ?? -->

	<% if user_signed_in? %>
		<% if comment.user_id == current_user.id? %>
			
			<%= link_to "Edit", edit_recipe_comment_path(comment.recipe, comment) %>
			<%= link_to "Delete", recipe_comment_path(comment.recipe, comment), method: :delete, data: {confirm: "Are you sure?"} %><br>
	 <% end %>
	<% end %> 